program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var n,o:int64; da,db:integer;
begin
  readln(n);
  if n mod 2=1  then o:=n*(n-1)
  else
   for da := 1 to n do
    for db := 1 to n do
       
         if (da+db) mod 2=1 then
         o:=o+1;
 writeln(o);
 readln;
end.
