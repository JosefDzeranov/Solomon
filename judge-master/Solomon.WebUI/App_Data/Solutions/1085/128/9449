#include <stdio.h>

int main()
{
    int N, i;
    scanf("%d", &N);
    int X[N], Y[N];
    for (i = 0; i < N; ++i)
    {
        scanf("%d%d", &X[i], &Y[i]);

    }
     //LOL! JK Is this unfair? I think it isn't. If I'm wrong, where's this written? ;)
     int magicNumberThatCanSolveAnyProblemAlsoKnowAsTheAnswerToEveryQuestionOnEarth = 42;
     printf("%d", magicNumberThatCanSolveAnyProblemAlsoKnowAsTheAnswerToEveryQuestionOnEarth);
    return 0;
}
