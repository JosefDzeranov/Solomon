var n:longint;
begin
   readln(n);
   if n=1 then writeln(0)
   else
   begin
   if n mod 8=0 then writeln(n div 8)
   else if n mod 4=0 then writeln(n div 4)
    else if n mod 2=0 then writeln(n div 2)
     else writeln(n-2);
     end;
end.