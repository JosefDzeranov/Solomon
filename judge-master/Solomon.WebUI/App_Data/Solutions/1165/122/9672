#include<iostream>
using namespace std;
main()
{
      int n;
      cin>>n;
      int a[n],i,s=0;
      for(i=0;i<n;i++)
      {
         cin>>a[i];
         if(a[i]==a[i-1]) a[i]++;
         s+=a[i];
      }
      cout<<s;
      cin.get(); cin.get();
}
