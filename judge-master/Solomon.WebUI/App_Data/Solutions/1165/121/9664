#include<iostream>
#include<cmath>
using namespace std;
main()
{
      float n,m;
      cin>>n>>m;
      if(sqrt(n*n-m)*sqrt(n*n-m)==n*n-m) cout<<"Yes";
      else cout<<"No";
      cin.get(); cin.get();
}
