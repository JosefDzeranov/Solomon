var n,i,k,a,b:integer;
var s:string;
begin
readln(n);
for i:=1 to n do begin
readln(s);
k:=length(s);
for b:=1  to k do begin
if (word(s[b])>48) and  (word(s[b])<58) then  a:=a+ord(s[b])-48;
end;
end;
writeln(a);
end.
