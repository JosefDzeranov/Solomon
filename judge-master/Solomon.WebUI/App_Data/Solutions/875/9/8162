#include<string>
#include<sstream>
#include<iostream>
#include<cmath>

using namespace std;

int main()
{
	long m;
	long n,a,b,c;
	cin >> m >> n >> a >> b >> c;
	a=min(min(a,b),c);
	if (m==1)
	{
		cout << a-1;
		return 0;
	}
		cout << a;
	return 0;
}