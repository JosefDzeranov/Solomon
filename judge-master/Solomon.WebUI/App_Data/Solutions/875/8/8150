#include<string>
#include<sstream>
#include<iostream>

using namespace std;

int main()
{
	string str;
	getline(cin,str);
	stringstream sstr(str);
	
	double a,b;
	sstr >> a >> b;
	cout << a+b;
	return 0;
}