#include<iostream>
#include<string>

using namespace std;

int main(){
	string str,rstr;
	cin >> str;
	for(int i = 0;i<str.size();i++)
	{
		if(str[i]=='#'||str.size()!=0)
		{
			rstr.erase(rstr.size()-1,1);
		}
		else
		{
			rstr+=str[i];
		}
	}
	cout << rstr;
	return 0;
}