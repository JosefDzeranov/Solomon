#include <iostream>

using namespace std;

int main() {
	long long int a, k, b, m, x, one, two;
	cin >> a >> k >> b >> m >> x;
	one = (k - 1) * m;
	two = (m - 1) * k;
	cout << (k * m * x)/(one * a + two * b);
	return 0;
}
