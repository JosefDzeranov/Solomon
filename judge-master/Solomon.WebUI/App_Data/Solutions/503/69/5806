program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;
var a,b,x:integer; v,t1,t2:extended;
begin
 readln(a,b,x);
 t1:= x/1000;
 if t1<=a then v:=1000 else
 begin
   t2:=b-t1;
   v:=x/(t1+t2);
 end;
writeln(v);
end.
