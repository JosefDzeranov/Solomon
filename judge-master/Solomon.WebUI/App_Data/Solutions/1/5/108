#include <iostream>
using namespace std;
int main()
{
	int n, k, s=1, ss=1, sss=1;
	cin >> n >> k;
	for (int i=1; i<=n; i++)
		s = s * i;
	for (int i=1; i<=k; i++)
		ss = ss * i;
	for (int i=1; i<=n-k; i++)
		sss = sss * i;
	s = s/(ss*sss);
	cout << s;
}
