program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var a,b,c,d,pp,i,q,o:integer;
begin
 readln(a,b,c,d);
 o:=-1;

 for I := c to d do
  begin
   pp:=i div 2;
   for q := 1 to pp div 2 do
    if (q*(pp-q)>=a) and (q*(pp-q)<=b) then o:=o+1;
  end;

 writeln(o);
readln;
end.
