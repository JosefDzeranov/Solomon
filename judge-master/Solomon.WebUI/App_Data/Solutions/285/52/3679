program olimp1; 
var N: byte;
    x, y, z: integer;
begin
    read(N);
    if(3<=N)and(N<=100)then begin
      y:=N-1;
      z:=N-2;
      x:= n*y*z;
      write(x);
      end
    else write('Неверно');
   
    end.