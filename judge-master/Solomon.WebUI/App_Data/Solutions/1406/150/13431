#include<stdio.h>
#include<string.h>

int main()
{
  //freopen("dit.in","r",stdin);
  //freopen("dit.out","w",stdout);
  
  char str[101];
  int r,i,s;
  
  scanf("%s",&str);
  
  r=strlen(str);
  s=0;
  for(i=0;i<r;i++)
    if(str[i]=='*') s+=i;
  
  printf("%d\n",s);
  return 0;
}
