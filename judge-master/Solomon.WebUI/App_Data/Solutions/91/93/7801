#include <iostream>

using namespace std;

int main()
{
    int x, y, z, q;
    cin >> x >> y;
    cin >> z >> q;
    int f = (z * 60 + q) - (x * 60 + y);
    cout << f / 60 << " " << f % 60;
    return 0;
}
