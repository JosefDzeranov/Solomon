program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var
    n, k, i, j: integer;
    stol: array [1..10000] of integer;
    rast: array [1..10000] of integer;
begin
 readln(n, k);
 for I := 1 to n do
 readln(stol[i]);
  writeln(1);
  readln;
end.
