var x1,x2,x3,y1,y2,y3,a,b,min,man2:integer;
BEgin
 readln(x1,y1);
 readln(x2,y2);
 readln(x3,y3);
 a:=abs(x3-x1);
 b:=abs(x3-x2);
 if a<=b then min:=a else min:=b;
 a:=abs(y3-y1);
 b:=abs(y3-y2);
 if a<=b then man2:=a else man2:=b;
 if min<=man2 then writeln(min) else writeln(man2);
 end.