#include <stdio.h>
#include <conio.h>
#include <math.h>
int main()
{
	float n, m, x;
	scanf ("%f%f", &n, &m);
	n=n*n-m;
	x=floor(sqrt(n));
	if (x*x==n) printf ("Yes");
	else printf ("No");
	getch();
}