#include <iostream>
#include<math.h>
#include<algorithm>
#include<stdlib.h>
using namespace std;

int main()
{
    float a,b,x,t;
    float v;
    cin>>a>>b>>x;
    v=x/min(a,b);
    if(v<1000)
    {
        while(v<1000)
           v=x/(min(a,b)-1)
    }
   t=a+b;
   if (v>1000)
   {
       v=x/max(a,b);
     if(v<1000)
    {
        while(v<1000)
           v=x/(miax(a,b)-1)
    }
   }

 if (v>1000)

{
    while(v>1000)



    v=x/t;
    t=t+b;
if(v>1000)
{
    while(v>1000)




        t=t+a;
}
}
}

    cout <<v;
    return 0;
}
