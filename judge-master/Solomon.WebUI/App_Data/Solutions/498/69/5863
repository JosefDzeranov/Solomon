#include <iostream>

using namespace std;

int main()
{
	double a, b, n, x = 1;
	cin>>a>>b>>n;
	while(true)	
	{
		if((int(x)%int(a+b)==a || int(x)%int(a+b)==b) && (n/x<=1000.0))
		{
			break;
		}
		x++;
	}
	cout<<n/x;
  	return 0;
}

