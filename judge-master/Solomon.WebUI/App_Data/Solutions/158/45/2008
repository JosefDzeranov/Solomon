#include <iostream>
#include <math.h>

using namespace std;

int main()
{
  int n,r1,r2,l;
 cin>>n>>l>>r1>>r2;
 if(n>=l*l)
 {
     if(r1=2*r2){
     cout<<"YES"<<" "<<r1*3<<" "<<r2*3;}
     else
     {
         cout<<"NO";
     }

 }
if(n<l*l)
cout<<"NO";

    return 0;
}
