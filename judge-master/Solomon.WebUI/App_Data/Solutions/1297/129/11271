program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var a, b, c, s, i, k, m, n, r:integer;
ch:char;
begin
randomize;
  read(a,b);
  for i := 1 to a do
    begin
      for k := 1 to b do
        begin
          read(ch);
        end;
      readln;
    end;
  r := random(4);
  writeln(r);
  readln;
end.
