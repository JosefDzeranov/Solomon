#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int n,m,k;
  cin>>n>>m;
  if ((n*n<m)) cout<<"No";
  else
{
int h;
  k=n*n-m;
  h=sqrt(k);
  if ((h*h==k) && (n*n>=m)) cout<<"Yes";
  else cout<<"No";
}
    return 0;
}
