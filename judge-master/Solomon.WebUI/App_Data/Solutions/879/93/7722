#include <stdio.h>
#include <stdlib.h>

int main()
{
    int h1, m1, h2, m2;

    scanf("%d %d %d %d", &h1, &m1, &h2, &m2);

    int min1 = h1*60+m1, min2 = h2*60+m2;
    int delta = min2-min1;

    printf("%d %d\n", delta/60, delta%60);

    return 0;
}
