#include <iostream>
#include <string>

using namespace std;

int main() {


	string s = "NO";
	int n = 3, k;
	cin >> k;
	for (int i = 0; i < k; i++)
	{
		int a, b;
		cin >> a >> b;
		n -= a;
		if (n <= 0)
			s = "YES";
		n += b;
	}
	cout << s << endl;


	return 0;
}