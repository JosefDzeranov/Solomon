#include <iostream>

using namespace std;

int main() {

	long long int n;
	long long int ans = 0;
	cin >> n;

	ans += 4 * n * n + 1;

	n = n * 2 - 1;

	long long int sum = (1 + n * n) * n / 2;
	ans += 2 * sum;

	cout << ans << endl;

	return 0;
}