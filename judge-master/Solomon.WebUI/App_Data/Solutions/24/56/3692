#include <iostream>

using namespace std;


int main() {

	int n;
	long long int ans = 0;
	cin >> n;
	for (int i = 2; i <= n; i++)
	{
		int f = i;
		while (f % 2 == 0)
		{
			ans++;
			f /= 2;
		}
			

	}
	cout << ans << endl;


	return 0;
}