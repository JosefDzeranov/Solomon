Module Module1

    Sub Main()
        Dim a, b, sum As Long
        Dim numbers As Array

        numbers = Console.ReadLine().Split(" ")
        a = numbers(0)
        b = numbers(1)

        sum = a + b

        Console.WriteLine(sum)

        Console.Read()
    End Sub

End Module
