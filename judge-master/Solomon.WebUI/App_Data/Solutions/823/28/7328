program Sale;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var
  a, b, c, s : integer;
begin
  s := 0;
  read (a, b, c);
  if a > b then
    if b > c then
      s := a + b
    else
      s := a + c
  else
    if a > c then
      s := a + b
    else
      s := b + c;
  write (s);
end.
