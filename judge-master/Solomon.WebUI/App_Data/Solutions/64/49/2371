#include <iostream>

using namespace std;


int main()
{
	int k, t, n;
	cin >> k;
	int c = 3;
	for (int i = 0; i < k; i++)
	{
		cin >> t >> n;
		c -= t;
		if (c != 0) c += n;
	}
	if (c == 0) cout << "YES";
	else cout << "NO";
	return 0;
}