#include <iostream>
#include <cmath>
#include <string>
#include <algorithm>
#include <vector>
#include <deque>
#include <map>
#include <stdlib.h>
#include <time.h>
using namespace std;

int main()
{
	//srand(time(NULL)); // 2 10 4 8
	//int n = rand() % 100;
	int n;
	cin >> n;
	if (n % 2 == 0)
		cout << n * (n - 2);
	else
		cout << n * (n - 1);
	return 0;
}