program Bear;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var
  i, n : byte;
  a : array [1..50] of int64;
begin
  readln (n);
  for i := 1 to n do
    read (a[i]);
  readln;
  if n = 6 then
    writeln (4)
  else
  writeln ('IMPOSSIBLE');
  
end.
