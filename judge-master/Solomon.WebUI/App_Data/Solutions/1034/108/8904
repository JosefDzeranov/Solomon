#include <stdio.h>
#include <stdlib.h>
int main(){

	long long n,i,s,k;
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%d",&k);
		if(i%2==0)s-=k;
		if(i%2==1)s+=k;
	}
	if(s>0)printf("0");
	if(s<0)printf("%d",1-s);
	return 0;
}