#include<iostream>
#include<math.h>
using namespace std;
int main(){
int64_t a,b,c,d;

if(a != 0 ||b != 0 ||c != 0 ||d != 0 ){
cin>>a>>b>>c>>d;
int64_t g=0;
g = min(a,b) + min(c,d);
if(g>0){
if(g != 2){
cout << int(sqrt(g));}
else cout<<'0';}
else
    cout<<'0';}
    else cout <<'0';

return 0;
}

