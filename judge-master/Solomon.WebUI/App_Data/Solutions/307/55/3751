var x,y,n:int64;
 begin
 // assign(input,'input1.txt'); reset(input);
 //  assign(output,'output1.txt'); rewrite(output);
    readln(n);
    x:=n*2-1;
    y:=(x*x)*(x*x+1)div 2;
    y:=y div x;
    writeln(2*y+1+2*n*n*2);
end.


