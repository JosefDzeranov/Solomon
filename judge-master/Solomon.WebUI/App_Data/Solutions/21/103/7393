#include <iostream>
using namespace std;
int main(){
	int n,a,sum=0;
	cin>>n;
	for (int i=0;i<n;i++)
	{
		cin>>a;
		if (i%2==0) sum+=a;
		else sum-=a;
	}
	if (sum>0) cout<<0;
	else cout<<1-sum;
	return 0;
}