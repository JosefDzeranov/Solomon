#include <iostream>
#include <cmath>
#include <string.h>

using namespace std;

/*int sym(char a)
{
    if ('0'<=a && '9'>=a) return (int(a))-(int('0'));
    else if (a=='*' || a=='/' || a=='+' || a=='-') return -1;
    else if (a=='=') return -2;
    else return -3;
}
*/
int main()
{
    string s;
    //int a,b,c,d;
    cin>>s;
    /*for (int i=0;i<s.length && d!=-3;i++)
    {
        d=sym(s[i]);
        if ()
    }*/
    cout<<"ERROR";
    return 0;
}
