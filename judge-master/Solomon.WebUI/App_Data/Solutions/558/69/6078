#include <iostream>

using namespace std;

int main() {

	double a, b, x;
	int t;
	cin >> a >> b >> x;
	t = x / 1000.0;
	int c = int(t) % int(a + b);

	if (c > a)
		t += b - c;

	double ans;

	ans = x / t;

	cout << ans << endl;


	return 0;
}