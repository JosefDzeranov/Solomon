#include <iostream>

using namespace std;

int main() {
	int n, a, b, c;
	cin >> n;
	a = n / 3;
	b = n / 3;
	c = n - a - b;
	if (b + 1 < c) {
		a--;
		b++;
	}
	else {
		a--;
		c++;
	}
	cout << a << " " << b << " " << c;
	return 0;
}