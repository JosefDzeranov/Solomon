#include <iostream>
#include <string>

using namespace std;

int main()
{
    string s1, s2;
    cin >> s1 >> s2;

    if (s2.length() > s1.length()) {
        cout << "IMPOSSIBLE" << endl;
        return 0;
    }

    int tl = s1.length() - s2.length();
    int *razn = new int[s2.length()-1];
    for (int i = 0; i <= tl; i++) {

    }

    return 0;
}
