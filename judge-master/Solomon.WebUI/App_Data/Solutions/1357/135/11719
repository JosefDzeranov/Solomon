#include <iostream>
#include <cmath>
#include <string>
using namespace std;

int main()
{
    long long a,b,c,d,x,y=1;
    cin>>a>>b>>c>>d>>x;
    while (x>0)
    {
        if (y%b!=0)
        {
            x=x-a;
        }
        if (y%d!=0)
        {
            x=x-b;
        }
        y++;
    }
    cout<<y;
    return 0;
}
