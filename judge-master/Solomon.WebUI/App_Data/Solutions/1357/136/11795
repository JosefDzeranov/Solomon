#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int n,k;
    cin>>n>>k;
    int x[k],y[k];
    for (int i=0; i<k; i++)
    {
        cin>>x[i]>>y[i];


    }
    if (k==n)
    cout<<0;
    return 0;
}
