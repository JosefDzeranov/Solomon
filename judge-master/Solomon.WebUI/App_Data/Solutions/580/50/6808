s = input().split();
n = int(input());
x = int(s[0]);
y = int(s[1]);
r = s[0] + s[1];
if(n > len(r)):
    while len(r) < n:
        c = int(x) + int(y);
        x = y;
        y = c;
        r = r + str(c);
    print(r[len(r)-1]);
else:
    print(r[n-1]);

