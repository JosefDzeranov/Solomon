program mesi8;
var n, l, r : integer;

begin
read(n, l, r);
   if ((l/(2*r)) < 3)then writeln('NO') else
if ((pi*l) > n) then writeln('NO')
  else 
    begin
    writeln('YES');
    if (l mod (2*r) = 0) then writeln(l/(2*r))
      else writeln((l div (2*r)) + 1);
    end;
end.