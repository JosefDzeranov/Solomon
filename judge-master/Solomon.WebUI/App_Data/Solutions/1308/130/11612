#include<iostream>

using namespace std;
typedef uint unsigned int;
int main(){
 	double a,b,c,d,r(0);
	cin >> a >> b >> c >>d;
	for(int i(0);i<=d;i++)
		for(int j(d);j>=a;j--)
		{
			int p(2*(i+j)),s(i*j);
			if(p>=c && p<=d && s>=a && s<=b)
				r++;
		}
	cout << r;
}