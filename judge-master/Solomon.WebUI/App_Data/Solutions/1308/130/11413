#include<iostream>

using namespace std;
int main(){
	int a,b,c,d,r(0);
	cin >> a >> b >> c >> d;
	for(int i(1);i<=(int)b/2+1;i++)
		for(int j((int)b/2+1);j>=a;j++)
			if((2*(c+d))>=c && (2*(c+d))<=d)
				r++;
	cout << r;
}