#include<iostream>

using namespace std;
int main(){
	unsigned long a,b,c,d,r(0);
	cin >> a >> b >> c >>d;
	for(int i(1);i<=d;i++)
		for(int j(d);j>=1;j--)
		{
			int s(i*j),p(2*(i+j));
			if(s>=a && s<=b && c<=p && d>=p)
				r++;
		}
	r=r/2+1;
	cout << r;
	system("pause >> null");
}