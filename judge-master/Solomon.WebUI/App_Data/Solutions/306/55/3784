var n,m,i,j,k,ans:longint;
begin
//  {$ifndef ONLINE_JUDGE}
//  assign(input,'input.txt'); reset(input);
//    assign(output,'output.txt'); rewrite(output);
//  {$endif}
  readln(n); ans:=0; m:=2*n-1; writeln(((1+m*m)div 2)*m*2+sqr(2*n)+1);
end.