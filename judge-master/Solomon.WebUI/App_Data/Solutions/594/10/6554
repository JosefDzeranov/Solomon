#include<iostream>
using namespace std;
int v,c,n,k;
int64_t res;
int64_t fact(int n)
{
    v=1;
    for(c=1;c<=n;c++)
    {
        v=v*c;
    }
    return v;
}

int main()
{
   cin>>n>>k;
   res=fact(n)/fact(k)/fact(n-k);
   cout<<res;
   return 0;
}
