#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	long long int a, b, c, d, k=0, n, i;
	cin>>a>>b>>c>>d;
	if(a>=b)	k+=b;
	else	k+=a;
	if(c>=d)	k+=d;
	else	k+=c;
	cout<<int(sqrtl(k));
	return 0;
}
