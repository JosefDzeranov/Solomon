program afdg;
  var n,a,b,c,k:integer;
  
  function ph(a:integer):integer;
    begin
      if a<>1 then ph:=a-1 else ph:=1;
    end;
  
  begin
  
  
    readln(k);
    readln(n,a,b,c);
    if k=2 then writeln(min(a,min(b,c)))
      else writeln(ph(min(a,min(b,c))));
  
  
  end.