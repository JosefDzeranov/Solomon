var
i,j,n,kn,k:integer;
begin
  readln(n);
  kn:=0;
  for i:=1 to n do
  begin
    j:=i;
    k:=0;
    while j mod 2 = 0 do
    begin
      inc(k);
      j:=j div 2;
    end;
    kn:=kn+k;
  end;
  writeln(kn);
end.  