program Project1;                       //задача 2

{$APPTYPE CONSOLE}

uses
  SysUtils;

var a,b,k,m,t,x:integer ;
begin
  read(a);
  read(k);
  read(b);
  read(m);
  readln(x);
  t:=0;
  while ((a+b)*t-(t div k)*a-(t div m)*b)<x do t:=t+1;
  writeln(t);
end.
