#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    long int a[4];
    cin >> a[0] >> a[1] >> a[2] >> a[3];
    cout << long(sqrt(min(a[0], a[1]) + min(a[2], a[3])));
    return 0;
}
