#include <stdio.h>
#include <conio.h>
int main()
{
    int a,b,i,s=1;
    scanf("%d",&a);
    if(a!=1)
   { for(i=0;i<a-2;i++)
    s=s+2;
    
    a=(a+s)*(a+s);}
    printf("%d",a);
  //  getch();
}
