   Program C;
   var N,k,i,m:integer;
   Begin
   readln(N);
   k:=0;
  for i:=1 to N do begin
  M:=i;
  if (M mod 2) =0 then begin
  repeat
  M:=M div 2;
  k:=k+1;
  until (M mod 2)=1;
  end
  end;
  writeln(k);
  End.
  