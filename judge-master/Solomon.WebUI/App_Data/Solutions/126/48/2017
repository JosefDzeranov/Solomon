#include <iostream>
using namespace std;
int main()
{
int a,b,c,d,f,w,r;
cin>>a>>b>>c>>d>>f;
if(b<0)
b=b-b*(-2);
if(c<0)
c=c-c*(-2);
if(d<0)
d=d-d*(-2);
if(f<0)
{f=f-f*(-2);}
w=b+d;
r=c+f;
if(a>w/2&&a>r/2)
cout<<w*r;
else
cout<<a*a;
return 0;
}
