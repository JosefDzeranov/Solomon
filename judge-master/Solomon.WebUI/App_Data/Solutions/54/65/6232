program Project2;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var a1,a2,a3,a4,k:integer; m:extended;
begin
  readln(a1,a2,a3,a4);
  k:=0;
   if a1<a2 then k:=k+a1
     else k:=k+a2;
       if a3<a4 then k:=k+a3
     else k:=k+a4;
     m:=SQRT(k);
     k:=TRUNC(m);
  writeln(k);
  readln;
 end.
