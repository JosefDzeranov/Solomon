#include<iostream>
#include<cmath>
using namespace std;
main()
{
      float N , M,k,l;
      cin>>N>>M;
      
      if ( (N*N-M)==sqrt(N*N-M)*sqrt(N*N-M))
      cout << "Yes";
      else cout << "No";

      }
      
