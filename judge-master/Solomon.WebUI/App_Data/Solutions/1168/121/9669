#include<iostream>
#include<cmath>
using namespace std;
main()
{
     float N,M,c,k;
      cin>>N>>M;
      c=N*N;
      k=sqrt(c-M);
      if(k*k==c-M)cout<<"Yes";
      else cout<<"No";
      }
      
