#include<iostream>
#include<string>
#include<stdio.h>

using namespace std;

int main()
{
	int n; cin >> n;
	string s = "";
	while (n != 1)
	{
		if (n % 2 == 0)
		{
			s = "*" + s; n /= 2;
		}
		else
		{
			s = "+" + s; n -= 1;
		}
		
	}
	cout << s;
	return 0;
}