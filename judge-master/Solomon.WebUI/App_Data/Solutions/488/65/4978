#include <iostream>

using namespace std;

int main()
{
	long long int a1, a2, a3, a4;
	cin >> a1;
	cin >> a2;
	cin >> a3;
	cin >> a4;
	long long int num1, num2;
	num1 = min(a1, a2);
	num2 = min(a3, a4);
	cout << long long int(sqrt(double(num1 + num2)));
	//system("pause");
	return 0;
}