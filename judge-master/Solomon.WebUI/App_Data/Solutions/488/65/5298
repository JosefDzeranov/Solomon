#include <iostream>

using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;
    int x, y, z1, z2, z3, z4;
    for (int i = 0; i < n; i++)
        cin >> z1 >> z2 >> z3 >> z4;

    for (int i = 0; i < k; i++)
    cin >> x >> y;
    cout << "NO";
    return 0;
}

