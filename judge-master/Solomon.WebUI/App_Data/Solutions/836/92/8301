program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var x1,x2,x3,y1,y2,y3,min,a,b,c:integer    ;
begin
readln(x1,y1);
readln(x2,y2);
readln(x3,y3);
a:=x3-x1;
b:=x2-x3;
c:=y2-y3;
min:=y3-y1;
if a<min then min:=a;
if b<min then min:=b ;
if c<min then min:=c;
writeln(min);
readln;
end.
