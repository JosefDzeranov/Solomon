#include <iostream>
#include <time.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int x, y;
        cin >> x >> y;
    }
    n = time(0) % 100;
    cout << n;
    return 0;
}
