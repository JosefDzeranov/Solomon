program Project1;

{$APPTYPE CONSOLE}

uses
  SysUtils;

var
  d,k:integer;
  n1,n2:real;
begin
 readln(k);
  d:=1+4*2*k;
   if d>=0 then
    n2:= (-1+sqrt(d))/2;
    writeln(trunc(n2));
    readln;
end.
