//#include "stdafx.h"
#include <iostream>
#include <math>
using namespace std;


int main()
{
	int n,m = 0;
	cin >> n;
	m = n - 1;
	cout << (int)(m*(pow(m,2) + 1) + pow(n, 2) + 1);
	//cin >> n;
	return 0;
}

