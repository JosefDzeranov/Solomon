program koza;
var
 L,x1,y1,x2,y2,lS,oS:longint;
begin
 readln(L);
 readln(x1,y1);
 readln(x2,y2);
 lS:=round(sqr(L)/1*pi);
 oS:=abs(x2-x1)*abs(y2-y1);
 if oS<lS then write(oS)
  else write(lS);
 readln;
end.