#include<iostream>
#include<cmath>
using namespace std;
int main() {
	unsigned long int a1, a2, a3, a4, b, r;
	cin >> a1 >> a2 >> a3 >> a4;
	if (a1>a2)
		b=a2;
	else
		b=a1;
	if (a3>a4)
		b+=a4;
	else
		b+=a3;
	r=sqrt(b);
	cout << r;
	cin.ignore();
	cin.get();
	return 0;
}