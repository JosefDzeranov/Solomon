#include <iostream>
#include <string>
#include <conio.h>
using namespace std;
int main(){
	int N,i,c=1,n,k=0;
	cin>>N;

	if(N%2!=0)N--;
	if(N%7==0)k=N-3;
	else if(N%10==0 || N%6==0)k=N-2;
	else if(N%8==0 || N%4==0 || N%2==0)k=N-1;
	if(N==0)k=0;
	cout<<k;

	cin>>N;
	return 0;
}