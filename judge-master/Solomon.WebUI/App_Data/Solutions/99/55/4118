#include <iostream>
#include <string>
#include <conio.h>
using namespace std;
int main(){
	int N,k1,k2,c=0,k;
	cin>>N;

	k1=N*2*N*2+1;
	k2=(2*N-1)*(2*N-1);
	for(int i=1;i<=k2;i++)
		c+=i;
	k2=c/(2*N-1);

	k=k2+k2+k1;;

	cout<<k;
	cin>>N;
	return 0;
}